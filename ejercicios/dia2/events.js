/**
 * Mandar un mensaje de alerta cuando la forma
 * haga submit
 **/

/**
 * Agregar un nuevo elemento li al final a la lista con clase
 * numbered-list cuando ocurra el evento hover
 **/

/**
 * Validar en el evento focusout que el campo con ID ticket_details
 * contenga solo números, si no cumple la condición entonces agregar
 * un <span>error</span> después del campo
 * HINT: /^\d+$/.test('your_string'); para verificar que contenga
 * solo números
 **/
